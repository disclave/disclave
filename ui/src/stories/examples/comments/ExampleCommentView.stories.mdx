import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { PageCommentsContainer } from "@/components/comments";
import { RandomCommentsList } from "../../data/PageComments";
import { EmptyActionHandler } from "../../data/CommentActionsHandler";
import { commentsTimestampComparator } from "../../data/helpers";

<Meta title="Examples/Comments/View" />

# Comments Container

Using comments container between other page components.

<Canvas>
  <Story name="Between other components">
    <div className="py-3 bg-yellow-100">
      Page header
    </div>
    <PageCommentsContainer
      className="max-h-64"
      comments={RandomCommentsList(50).sort(commentsTimestampComparator)}
      authenticated={true}
      onSubmit={() => {}}
      onLogin={() => {}}
      commentsActionsHandler={EmptyActionHandler}
    />
    <div className="py-3 bg-yellow-100">
      Page footer
    </div>
  </Story>
</Canvas>